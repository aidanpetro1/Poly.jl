# RETIRED — diagnostic script used during v0.4 perf-bisection (2026-05-02).
# The cofree_comonoid eager-subst bug it surfaced is now fixed in
# `src/Cofree.jl` (use of `subst_lazy` for the duplicator's codomain).
# Safe to delete; kept for one minor as a marker.
